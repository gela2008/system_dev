# 振り返り報告書

## メンバー情報
| 氏名 | GitHub ID | 学籍番号 |
|---|---|---|
| 戸田陽菜 | gela2008 | i23027 |

## 担当箇所・貢献内容
本課題では一人でシステム開発を行ったため、要件定義、基本設計、詳細設計、データベース設計、PHPによる実装、GitHubでのブランチ管理、Issue・Pull Requestの作成、READMEの作成まで全て担当した。

## KPT
| 週 | Keep | Problem | Try |
|---|---|---|---|
| 第1週 | GitHubリポジトリを作成し、開発環境を整えられた。 | Gitやブランチ操作に慣れておらず、操作に時間がかかった。 | Gitの基本操作を確認しながら開発を進める。 |
| 第2週 | 要件定義書と基本設計書を作成できた。 | システム全体の機能整理に時間がかかった。 | 機能ごとに優先順位を付けて設計する。 |
| 第3週 | SQLiteとPHPを用いたログイン機能やTodo管理機能を実装できた。 | データベース接続やテーブル構成でエラーが発生した。 | エラーメッセージを確認し、原因を一つずつ切り分けて修正する。 |
| 第4週 | カレンダー表示、Todo追加・編集・削除機能を実装できた。 | コード量が増え、修正箇所の把握が難しくなった。 | モジュールごとに役割を分割して管理する。 |
| 第5週 | アプリ内お知らせ機能やREADMEを完成できた。 | Gitのブランチを誤って作成してしまった。 | cherry-pickやPull Requestを利用して履歴を整理する。 |
| 第6週 | 設計書やREADMEなど提出物を完成させた。 | 提出条件の確認に時間がかかった。 | 提出要件を早めに確認し、計画的に資料を作成する。 |


## うまくいったこと
設計を先に行ってから実装へ進めたことで、大きな仕様変更をすることなく開発を進めることができた。AIを活用することにより、ログイン機能、カレンダー表示、Todoの追加・編集・削除、など、当初予定していた主要機能をスムーズに実装できた。
途中で誤ったブランチにコミットしてしまうトラブルが発生したが、`git cherry-pick`を利用してコミットを新しいブランチへ移動し、履歴を整理したうえでPull Requestを作成した。その結果、コミット履歴を崩さずに開発を継続することができた。

## 課題と改善案
実装途中でデータベース設計や画面構成を変更する場面があり、修正に時間を要した。
次回同様の開発を行う場合は、設計段階で画面やデータベース構成をより詳細に決定し、実装前の確認を十分に行いたい。

# AI活用記録
| 日時 | プロンプト | AIの出力内容 | 採否 | 理由 |
|---|---|---|---|---|
| 2026/7/16 | 「追加画面でタスク・スケジュールを選択したら表示内容を変えたい。」 | `add.php`にラジオボタンを配置し、「タスク」が選択されたときだけ科目と着手予定日の入力欄を表示するようにしましょう。まずHTMLを修正し、その後JavaScriptで表示・非表示を切り替えます。最後にPHP側でタスクとスケジュールの保存処理を分けて動作確認してください。 | 採用 | 手順どおりに実装し、希望する画面構成を実現できたため。 |
| 2026/7/19 | 「insert_subjects.phpの中身を確認してもらえますか。」 | `insert_subjects.php`の内容を確認しました。コードに大きな問題はありません。次に`setup_db.php`と`check_db.php`を確認し、PowerShellでデータベースのテーブル一覧を表示してみましょう。`test.db`の場所が異なっている可能性があるため、接続先を確認してから再度実行してください。 | 採用 | 原因を特定でき、科目一覧が正常に表示されるようになったため。 |
| 2026/7/24 | 「アプリ内お知らせ機能を実装したい。」 | `todo`テーブルの`date`、`start_date`、`completed`を利用してお知らせを自動生成しましょう。「今日着手予定」「今日締切」「明日締切」の3種類をSQLで取得し、画面右上のメールアイコンに未読件数を表示します。件名や本文はPHPで条件分岐して表示すれば実装できます。 | 加筆 | 表示デザインは変更したが、処理内容はそのまま利用したため。 |
| 2026/7/27 | 「notice.phpを確認してください。」 | `notice.php`のコードを確認しました。PowerShellで文字化けして見えていますが、ファイル自体が壊れているわけではありません。まずVSCodeで文字コードがUTF-8になっていることを確認してください。その後、`title`タグや本文中の文字化けした文字列を日本語へ修正しましょう。修正後に画面を表示して、お知らせが正しく表示されるか確認してください。 | 採用 | 文字化けの原因を特定でき、正常に表示されるようになったため。 |
| 2026/7/27 | 「GitHubのブランチを切り直したい。」 | まず`master`へ切り替えて最新の状態を取得してください。その後、新しいブランチを作成し、`git cherry-pick`でコミットを移動します。移動が終わったら古いブランチを削除し、新しいブランチをPushしてPull Requestを作成しましょう。最後にMerge後、`master`を最新化して作業完了です。 | 採用 | コミット履歴を崩さずにブランチを修正できたため。 |

## AI活用を通じた学び
開発中は生成AIを活用し、PHPやSQLiteのエラー原因の確認、GitHubの操作手順、設計書の構成、READMEの作成などについて助言させた。
AIから得られた内容をそのまま利用するのではなく、自分で実装内容や授業資料と照らし合わせながら必要な部分だけを採用したことで、理解を深めながら開発を進めることができた。また、エラーが発生した際には原因を一緒に整理しながら解決できたため、問題解決の進め方も学ぶことができた。